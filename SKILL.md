---
name: ptp-transplant
description: 原型移植：将交互原型接入正式行为，或按原型重做旧页面。
---

# PTP Transplant

PTP = Prototype to Production。先确定用户真正签收的目标，再把原型**页面结构**作为复制源，把正式契约和行为作为数据源；不要从旧视图自由生长一套“相似”UI。Skill 分为用户向的大流程和工程向的小流程。

## 启动时：确认能力与任务形状

在读取项目代码之前确认四项能力：

| 显式依赖 | 用途 | 缺失时 |
|---|---|---|
| `grilling` skill | 与用户逐个澄清产品取舍、哪些 demo 偏离可接受 | 说明缺失并问用户是否继续采用普通问答对齐；不要声称做过 grilling |
| `to-tickets` skill | 把已确认范围拆成有阻塞关系、可验收的页面票 | 说明缺失并问是否改用现有 tracker/手工票；若该 skill 禁止模型主动调用，请用户显式运行 `/to-tickets`，不要冒充已经立票 |
| `browser-use` | 主 agent 打开真实页面、复演、取证 | 说明缺失并问是否继续只做准备/代码工作；不得宣称通过真实交互门 |
| 截图能力 | 用同视口、同状态的原型与正式页面核对结构 | 说明缺失并问是否继续；不能用 DOM 或单测冒充视觉验收 |

还要确认能否让**未参与该区域实现的审阅者只读截图**。若不能，不冒充独立审阅：说明缺口，询问用户是否亲自并排核对；未经其明确确认不标视觉门通过。

检查实际运行时能力，不靠工具名字猜测可用性；遵守宿主环境的 Skill 和浏览器调用规则。截图能力暂时故障与根本缺失不同，按 [工程约束](reference/construction-discipline.md) 处理，不无限重试。

项目规模没有统一门槛：有的项目用一行命令运行页面，有的需要隔离数据库、worker 和反向代理。确定**最小可运行路径**及是否存在写入副作用即可，不要求为小项目搭复杂环境。没有可复用的正式行为资产时，明确向用户问“本次要移植哪些行为”，不要自行发明后端合同；仍可先对齐原型与页面骨架。找不到精确签收 commit 时先确定可复现的目标版本（例如 tag、固定文件快照或用户指定版本），不要假定“最新”就是目标。

## 大流程 1：与用户对齐目标

在多种合理产品选择会改变页面或接线边界时，使用 `grilling` 逐问确认；事实、代码位置和技术实现由 agent 自己核查，别把可查事实塞给用户决策。最少对齐：

- 哪些**页面**及页面状态属于本轮；原型的固定版本和谁签收；是否需要保留旧页面；
- 哪些正式行为、URL/权限/错误/写路径必须保留；资产缺失时逐项问用户目标行为；
- 原型与正式事实不符时，哪些可见偏离要先改 demo 再同步；
- 页面验证口径和用户亲自验收时机；能写数据时使用哪套隔离环境；
- 哪些旧文档可改，哪些是冻结的事实基线，谁能批准覆盖。

把用户裁决写在当前任务票或决策记录里，注明覆盖的既有要求及测试后果；不把用户的建议自动升级成禁令。精确签收版本和版本变更只在用户确认后更新。停止追问已经可从代码或用户先前裁决推出的事实。

**进入规划的判据**：目标页面与固定原型版本、要接的正式行为、用户负责的取舍都已记录；未决项有明确阻塞范围，不用模糊的“先接着做”掩盖。

## 大流程 2：规划页面票，再交给小流程

范围确认后使用 `to-tickets` 建立页面所有权、依赖和完成条件；若宿主将该 skill 设为仅用户触发，提示用户显式运行 `/to-tickets`，不能暗中替代。小项目可一页一票；不要按 Header、Inspector、Composer 拆出彼此独立签收的票。票中写原型固定版本、要保留的正式行为、需要的隔离写路径、视觉与行为两道门，以及用户签收条件。

**每张页面票开工时**读取一次 [工程规划](reference/construction-plan.md)，确定工件和施工顺序。施工期间每次扩大范围、接受偏离、宣告通过和提交前都对照 [工程约束](reference/construction-discipline.md)。不要把规划文件当成每一步都要重做的检查表。

建议把工作记在项目既有任务系统；没有则在任务目录下按需建立：

- [原型区域清单](templates/demo-regions.md)：页面上有什么、在什么条件出现；
- [行为资产清单](templates/behavior-assets.md)：正式行为及其证据源；缺失行为先问用户；
- [移植底账](templates/transplant-ledger.md)：区域到正式 owner 的映射、施工状态与交互复演，**同一个进度事实源**；
- [测试分诊](templates/test-triage.md)：仅有绑定待替换视图的存量测试时使用；没有就不建该文件。

这些是可裁剪模板，不要求每个小项目新建四份文件。可将短清单直接嵌入票面，只要区域、行为、进度和复演结果有唯一位置且可核对。

**进入工程施工的判据**：页面票已指定唯一 owner、固定原型版本、行为边界、必要依赖以及视觉／行为门和用户签收条件；缺失能力导致无法执行的门已向用户说明并得到继续方式的裁决。

## 大流程 3：用户验收

工程小流程完成后向用户展示正式可访问页面与比较证据，邀请其**实际操作目标路径**；如未签收，不标记页面 done。交付说明分清：

1. 结构门证据（同视口、同页面状态的实际原型与正式产物）；
2. 正式行为证据（测试和必要的真实写路径）；
3. 交互底账逐条结果与明确的不适用项；
4. 用户已批准偏离、待批准偏离和未验证项。

**完成判据**：结构、正式行为、交互复演和用户签收各有独立结果；缺项保持未完成并报告原因。不用测试绿推断视觉通过，不用截图推断命令成功。经用户授权后安排 demo 缺陷分支回写到原型主分支；签收一个页面不自动授权合入、推送、改冻结文档或开始下一页面。

## 五条始终有效的底线

1. **不降级目标**：页面级源级移植不是在旧壳中替换几个组件。具体判据见工程约束。
2. **不混淆权威**：原型决定已签收的可见结构，正式合同决定事实和命令，用户决定取舍。
3. **不用自证代替实物**：构建、测试、代码量和实施者印象都不能独自证明页面保真。
4. **偏离先显式裁决**：正式要求与原型冲突时形成可看的目标状态，再决定移植；不要静默只改正式侧。
5. **按变化的风险验证**：骨架成形时验证结构；只改接线时验证行为；布局再变就补结构验证；收口复核整页。不要机械地每片重跑全套截图。

遇到本项目曾经出现的“旧壳换皮假完成”“有清单但施工没引用”“反复盲审占满时间”“截图通道卡死”等情形，再按需读 [失败模式与案例](reference/failure-modes.md)。
