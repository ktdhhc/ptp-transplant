# 正式行为资产清单 · <页面名>

- 正式语义权威：`<领域文档 / contract 位置>`
- 最小运行路径：`<单命令或已有隔离环境>`
- 写入授权目标：`<无写路径 / 隔离库及授权依据>`
- 若正式资产显式缺失，向用户确认本次要移植的行为和期待结果；不要因模板需要而创造后端字段。

| 行为／事实 | 正式入口 `file:line` | 契约与范围 | 页面消费点 | 已有回归证据 | 缺口／去向 |
|---|---|---|---|---|---|
| 页面对象读取 | `services/x.ts:line` | `GET …` + schema | 标题 | `test:line` | 无 |
| 模式切换与 URL | `url-state.ts:line` | query 的合法值与恢复 | 工具栏 | `test:line` | 无 |
| 写命令 | `services/x.ts:line` | 权限、状态、错误与最终回读 | Composer | `test:line` | 隔离环境实测 |
| … | … | … | … | … | … |

把“正式已提供”“仅前端派生”“正式未提供／未定”区分开；展示缺口不通过 mock 假造。没有正式行为资产的项目可把本表压缩为经用户确认的目标行为清单，并据此决定哪些功能本轮只做原型、哪些需要正式合同另立任务。
