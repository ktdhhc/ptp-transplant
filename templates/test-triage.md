# 存量测试分诊 · <页面名>

**仅在**有绑定待替换视图的存量测试时使用。若没有此类测试，不建空文档，在移植底账写一句“无存量视图测试可分诊”。

| 文件与用例 `file:line` | 行为主张 | keep / adapt / delete | 具体依据 | 处理与验证 |
|---|---|---|---|---|
| `View.test.ts:20` | 发送后保存真实 Run identity | keep | 命令与响应契约，不依赖旧 DOM | 原测试继续绿 |
| `View.test.ts:40` | 后退恢复选中的 Run | adapt | 旧 selector 绑死旧视图；URL 行为仍有效 | 新页面同义断言 |
| `View.test.ts:60` | 某组件内部 className 的像素尺寸 | delete | 只锁旧组件私有结构 | 由结构门负责 |

- keep：正式合同、服务调用、URL、权限、错误、状态机、命令等可复用行为。
- adapt：行为仍有效，仅旧 testid／组件入口／私有 DOM 观察方式作废；**不得降低行为断言强度**。
- delete：被判废视图的 DOM、CSS、第三方组件内部结构或固定坐标；写明原断言及为什么不再承载验收责任。
- 用户改变行为目标时，记录被覆盖的原要求和测试去向，不静默删除或反复维护注定变红的旧断言。
